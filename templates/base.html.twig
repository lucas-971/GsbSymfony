<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
        <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
        {% block navbar %}
            <!-- component -->
                <nav class="flex items-center justify-between flex-wrap bg-green-600 p-6">
                <div class="flex items-center flex-no-shrink text-white mr-6">
                    <img class="h-12 w-auto" src="images/gsblogo.png" alt="">
                </div>
                <div class="block lg:hidden">
                    <button class="flex items-center px-3 py-2 border rounded text-teal-lighter border-teal-light hover:text-white hover:border-white">
                    <svg class="h-3 w-3" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><title>Menu</title><path d="M0 3h20v2H0V3zm0 6h20v2H0V9zm0 6h20v2H0v-2z"/></svg>
                    </button>
                </div>
                <div class="w-full block flex-grow lg:flex lg:items-center lg:w-auto">
                    <div class="text-sm lg:flex-grow">
                    <a class="transition duration-500 hover:text-gray-400" href="{{ path('compte_rendu') }}">#Consulter comptes-rendus</a>
                    <a class="transition duration-500 hover:text-gray-400" href="{{ path('praticien') }}">#Consulter liste des praticiens</a>
                    </div>
                    <div>
                    <a class="transition duration-500 bg-red-600 w-auto flex justify-center rounded-md hover:bg-red-700 p-1" href="{{ path('logout') }}">Déconnexion</a>
                </div>
                </nav>
            
            
        {% endblock %}

        {% block content %}
        
        {% endblock %}

        {% block footer %}
            <div class="bg-gray-800 text-gray-200 flex justify-center p-2">
                
            </div>
        {% endblock %}
    </body>
</html>
