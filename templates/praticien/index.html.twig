{% extends 'base.html.twig' %}

{% block title %}Liste des praticiens{% endblock %}

{% block content %}
<!-- component -->
<br></br>
<div class="grid grid-cols-8">  
        

        {# <div class="flex items-center gap-5 col-span-7">
            <a class="transition duration-500 bg-red-400 hover:bg-blue-700 text-gray-200 p-1 mr-3 flex justify-center rounded-md w-full" href="">Confiance</a> 
            <a class="transition duration-500 bg-red-400 hover:bg-blue-700 text-gray-200 p-1 mr-3 flex justify-center rounded-md w-full" href="">Temps écoulé</a>
            <a class="transition duration-500 bg-red-400 hover:bg-blue-700 text-gray-200 p-1 mr-3 flex justify-center rounded-md w-full" href="">Notoriété</a>
        </div>      #}
    </div>
      <table class="min-w-full table-auto">
        <thead class="justify-between">
          <tr class="bg-green-600">
            
            <th class="px-16 py-2">
              <span class="text-gray-300">First Name</span>
            </th>
            <th class="px-16 py-2">
              <span class="text-gray-300">Name</span>
            </th>
            <th class="px-16 py-2">
              <span class="text-gray-300">adresse</span>
            </th>

            <th class="px-16 py-2">
              <span class="text-gray-300">Coef Confiance</span>
            </th>

            <th class="px-16 py-2">
              <span class="text-gray-300">ville</span>
            </th>
          </tr>
        </thead>
        <tbody class="bg-gray-200">
          <tr class="bg-white border-4 border-gray-200">
            
            <td>
            {% for praticien in praticiens %}
              <span class="text-center ml-2 font-semibold">
              <p>{{ praticien.nom }}</p>
            {% endfor %}
            </td>
            <td class="px-16 py-2">
              {% for praticien in praticiens %}
              <span class="text-center ml-2 font-semibold">
              <p>{{ praticien.prenom }}</p>
            {% endfor %}
            </td>
            <td class="px-16 py-2">
              {% for praticien in praticiens %}
              <span class="text-center ml-2 font-semibold">
              <p>{{ praticien.adresse }}</p>
            {% endfor %}
            </td>
            <td>
            {% for praticien in praticiens %}
              <span class="text-center ml-2 font-semibold">
              <p>{{ praticien.coefConfiance }}</p>
            {% endfor %}
            </td>
            <td>
            {% for praticien in praticiens %}
              <span class="text-center ml-2 font-semibold">
              <p>{{ praticien.ville }}</p>
            {% endfor %}
            <td >
              
            </td>
          </tr>
          
            
          </tr>
        </tbody>
      </table>
    </div>

{# <div class="bg-gray-300 m-10 border-2 border-gray-500">

    <div class="grid grid-cols-8">  
        <p class="p-2">
        Trier par : 
        </p>

        <div class="flex items-center gap-5 col-span-7">
            <a class="transition duration-500 bg-blue-600 hover:bg-blue-700 text-gray-200 p-1 mr-3 flex justify-center rounded-md w-full" href="">Confiance</a> 
            <a class="transition duration-500 bg-blue-600 hover:bg-blue-700 text-gray-200 p-1 mr-3 flex justify-center rounded-md w-full" href="">Temps écoulé</a>
            <a class="transition duration-500 bg-blue-600 hover:bg-blue-700 text-gray-200 p-1 mr-3 flex justify-center rounded-md w-full" href="">Notoriété</a>
        </div>     
    </div>

    <div class="grid grid-cols-2 m-5 bg-white p-2 border-2 border-gray-400">
        <!-- 1 -->
        <div>
            <div class="flex justify-center border-b border-t border-r border-gray-700">
                <p>Nom</p>
            </div>
            <!-- foreach -->
            <div class="flex justify-center border-b border-r border-gray-700">
                <p>Nom</p>
            </div>
        </div>
        <!-- 2 -->
        <div>
            <div class="flex justify-center border-b border-t border-gray-700">
                <p>Ville</p>
            </div>
            <!-- foreach -->
            <div class="flex justify-center border-b border-gray-700">
                <p>Ville</p>
            </div>
        </div>     
    </div>

</div> #}
{% endblock %}
